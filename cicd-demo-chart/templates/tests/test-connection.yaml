apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cicd-demo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cicd-demo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cicd-demo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
